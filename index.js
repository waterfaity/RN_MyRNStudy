/**
 * @format
 */

import { AppRegistry } from "react-native";
import { name as appName } from "./app.json";
import TestApp from "./myRNStudy";

AppRegistry.registerComponent(appName, () => TestApp);
